<section class="sample-text-area">
  <div class="container">
    <div class="row fullscreen align-items-center justify-content-center">
      <div class="col-md-6 banner-right card shadow-lg">
        <%= form_for resource, as: resource_name, url: password_path(resource_name), html: {method: :put, class: "form-wrap"}  do |f| %>
          <%= render "users/shared/error_messages", resource: resource %>
          <%= f.hidden_field :reset_password_token %>
          <%= f.password_field :password, class: "form-control", placeholder: "Enter password" %>
          <%= f.password_field :password_confirmation, class: "form-control", placeholder: "Enter password" %>
          <%= f.submit "Change my password", class: "primary-btn text-uppercase" %>
        <% end %>
      </div>
    </div>
  </div>
</section>
